---
title: "rpollock_pbmc_prediction"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r}
reticulate::use_python("/Users/erikstorrs/miniconda3/envs/test3/bin/python")
```



```{r}
library('Seurat')
library('rpollock')
```

## Including Plots

You can also embed plots, for example:

```{r}
pbmc = readRDS(('../tests/data/247669_processed.rds'))
pbmc
```
```{r}
fit_model(pbmc@assays$RNA@counts, pbmc@meta.data$seurat_clusters, '../tests/data/test_module')
```

```{r}
predictions = predict_cell_types(pbmc@assays$RNA@counts, '../tests/data/test_module/')

predictions
```